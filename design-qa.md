# Design QA

## 验收范围

- 视觉来源：`/var/folders/zp/740gyqgd0f5fcc681rzv263w0000gn/T/codex-clipboard-a76871db-ec17-486d-bd4c-efd810d84de2.png`
- 来源用途：用户指定的配色参考，不要求复制其业务内容和卡片结构。
- 实现页面：`http://127.0.0.1:4173/`
- 验收状态：真实超级管理员登录后的工作台；数据由当前 API 会话提供，工作台指标为首版展示数据。

## 比较证据

| 轮次 | 视口 | 页面状态 | 证据 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | 1886 × 679 | 登录页 | `/tmp/kratos-admin-desktop.png` 前的登录截图 | 通过；深灰渐变与红色主操作符合参考色系 |
| 2 | 1886 × 679 | 工作台顶部发生滚动 | 浏览器计算得到 `scrollY=50` | 非样式缺陷；恢复页面顶部后复验 |
| 3 | 1886 × 679 | 已登录工作台、页面顶部 | `/tmp/kratos-admin-desktop.png` | 通过；顶栏、激活线、页面背景、卡片和阴影匹配目标色系 |
| 4 | 390 × 844 | 刷新页面 | 自动 refresh 恢复失败 | 修复；refresh 响应补齐用户和租户上下文，路由启动时自动恢复会话 |
| 5 | 390 × 844 | 刷新后工作台 | `/tmp/kratos-admin-mobile.png` | 通过；双列指标卡、单列内容区无溢出 |
| 6 | 390 × 844 | 抽屉导航展开 | 浏览器交互截图 | 通过；抽屉宽 327.59px、遮罩正常、导航项可见 |
| 7 | 390 × 844 | 文件管理、演示租户 | 浏览器交互截图 | 通过；真实文件完成预登记、直传、服务端确认并显示“可用”，移动端只保留文件名与操作列 |
| 8 | 1886 × 679 | 图形验证码登录页 | 浏览器交互截图 | 通过；服务端 PNG 验证码加载正常，真实输入验证码后登录成功 |
| 9 | 768 × 768 | 登录日志、API 日志 | 浏览器语义快照 | 通过；真实登录事件、请求状态、耗时与 request ID 均由数据库返回，平台管理员在租户上下文可跨租户治理 |
| 10 | 1886 × 679 | API 日志导出与导出记录 | 浏览器语义快照 | 通过；Worker 真实生成 48 条平台范围 CSV，任务状态、导出历史和短期下载入口均可操作 |
| 11 | 1280 × 720 | 系统设置、演示租户 | 浏览器截图与语义快照 | 通过；三级配置来源、响应式卡片和敏感配置状态正常 |
| 12 | 1280 × 720 | 渠道配置、演示租户 | 浏览器截图与语义快照 | 通过；本地邮件 Provider 真实加密保存并完成连接测试 |

桌面端同视口对比图：`/tmp/kratos-admin-palette-compare.png`。左侧为参考，右侧为实现。

## 视觉结论

- 顶栏：灰色到深灰色渐变，白色导航文字，当前项使用红色下划线。
- 内容区：浅灰背景、白色卡片、低对比阴影，与参考图保持一致。
- 强调色：红色只用于当前导航、卡片标记、主操作和图表关键数据，未大面积滥用。
- 响应式：桌面顶部导航、手机菜单按钮与右侧抽屉均可操作。
- 文件链路：租户权限指令控制上传、下载与删除按钮；真实浏览器直传后列表立即刷新。
- 日志链路：登录标识已脱敏，API 日志不保存请求正文；错误响应头和响应体返回同一 request ID。
- 导出链路：页面创建异步任务后轮询状态，Worker 生成 UTF-8 CSV；导出历史可在刷新后继续查询与下载。
- 配置链路：代码、平台、租户三级来源清晰可见；敏感设置与 Provider 配置均不回显。
- Provider 链路：本地、SMTP、阿里云短信与 OSS 使用专用参数表单；连接测试不暴露密钥，短信测试不发送短信。
- 控制台：桌面与手机验收状态均无 error 或 warning。

## 最终结果

**PASS**：本阶段无 P0、P1、P2 视觉缺陷。页面业务内容按管理后台工作台需求实现，配色遵循用户提供的参考图。
